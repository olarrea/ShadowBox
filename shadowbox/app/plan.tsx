import { View, Text } from "react-native";

export default function Plan() {
  return (
    <View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
      <Text>Plan generado (pendiente)</Text>
    </View>
  );
}
